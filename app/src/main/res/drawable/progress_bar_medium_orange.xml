<!-- res/drawable/yellow_progress_bar.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The progress indicator line -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/black" /> <!-- Change this color to yellow (#FFEB3B) or your desired color -->
            </shape>
        </clip>
    </item>
</layer-list>
